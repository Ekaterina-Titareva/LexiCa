import styles from './footer.module.css'

function Footer() {
    return (
        <footer>
            <nav className={styles.nav}>
                Contact information
            </nav>
        </footer>
    )
}

export default Footer;